import { FullnamePipesPipe } from './fullname-pipes.pipe';

describe('FullnamePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new FullnamePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
