import { GenderPipesPipe } from './gender-pipes.pipe';

describe('GenderPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
